# Ветки git и их использование

Отображение всех веток
```sh
git branch
```

Переход между ветками
```sh
git checkout (имя новой ветки)
```

Слияние веток
```sh
git merge
```

Переход к изображению веток
```sh
git log --oneline --graph
```
